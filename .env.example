# Web Application Security Testing Configuration

# Target Configuration
# Replace with your authorized target URL
TARGET_URL=https://example.com

# Authentication (if needed for authenticated tests)
# NEVER commit real credentials - use test accounts only
TEST_USERNAME=
TEST_PASSWORD=
TEST_EMAIL=

# Security Tester Information
SECURITY_TESTER=Your Name

# Test Configuration
REQUEST_TIMEOUT=10
MAX_RETRIES=3
DELAY_BETWEEN_REQUESTS=0.5

# Report Configuration
REPORT_OUTPUT_DIR=reports
REPORT_FORMAT=both  # markdown, json, or both

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/security_tests.log

# Proxy Configuration (for tools like Burp Suite, OWASP ZAP)
# HTTP_PROXY=http://127.0.0.1:8080
# HTTPS_PROXY=http://127.0.0.1:8080

# Test Scope Limitations
ENABLE_AGGRESSIVE_TESTS=false
ENABLE_TIME_BASED_TESTS=false
ENABLE_DESTRUCTIVE_TESTS=false

# Rate Limiting (be respectful to target server)
REQUESTS_PER_SECOND=2
MAX_CONCURRENT_REQUESTS=5
